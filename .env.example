# Application Environment
NODE_ENV=development # development, preview, or production
VITE_APP_ENV=development # development, preview, or production

# Netlify Configuration
NETLIFY_SITE_ID=your-site-id-here # Required for all environments
NETLIFY_AUTH_TOKEN=your-auth-token-here # Required only in CI environment

# Build Configuration
CI=false # Set to true in CI environment 